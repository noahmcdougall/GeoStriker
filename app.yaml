version: 1
runtime: python
runtime_config:
  python_version: 3
api_version: 1
threadsafe: false
# [START handlers]
handlers:
- url: /.*
  script: /wsgi/application.py
# [END handlers]

# [START libraries]
#libraries:
# [END libraries]
